# Paywall App

Тестовое Flutter-приложение с онбордингом, экраном подписки и главным экраном.

## Стек технологий

- **Flutter** — фреймворк для разработки
- **Riverpod** — управление состоянием
- **GoRouter** — навигация
- **SharedPreferences** — локальное хранилище
- **Flutter Test** — юнит-тестирование

## Архитектура проекта

Проект использует простую трёхслойную архитектуру без избыточности:

```
┌─────────────────────────────────────────┐
│           UI Layer (Screens)            │  ← Презентация
│   - onboarding_screen.dart              │
│   - paywall_screen.dart                 │
│   - home_screen.dart                    │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      State Layer (Providers)            │  ← Бизнес-логика
│   - subscription_provider.dart          │
│   - onboarding_provider.dart            │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│       Data Layer (Services)             │  ← Работа с данными
│   - subscription_service.dart           │
│   - storage_service.dart                │
└─────────────────────────────────────────┘
```

### Принципы

- **Screens** — только UI и взаимодействие с пользователем
- **Providers** — управление состоянием через Riverpod, обработка событий
- **Services** — работа с SharedPreferences и эмуляция API
- **Models** — простые data-классы без логики
- **Router** — централизованная навигация через GoRouter

### Особенности

- Провайдеры читают данные из сервисов и обновляют UI
- Сервисы не знают о провайдерах и экранах
- Роутер управляет редиректами на основе состояния подписки
- Все сервисы покрыты юнит-тестами

## Структура проекта

```
paywall_app/
├── lib/
│   ├── main.dart                          # Точка входа, ProviderScope
│   │
│   ├── router/
│   │   └── app_router.dart                # GoRouter с redirect-логикой
│   │
│   ├── providers/
│   │   ├── subscription_provider.dart     # Состояние подписки
│   │   └── onboarding_provider.dart       # Состояние онбординга
│   │
│   ├── services/
│   │   ├── subscription_service.dart      # Эмуляция покупки подписки
│   │   └── storage_service.dart           # Работа с SharedPreferences
│   │
│   ├── screens/
│   │   ├── onboarding/
│   │   │   └── onboarding_screen.dart     # 2 экрана онбординга
│   │   ├── paywall/
│   │   │   └── paywall_screen.dart        # Экран выбора подписки
│   │   └── home/
│   │       └── home_screen.dart           # Главный экран с контентом
│   │
│   └── models/
│       └── subscription_plan.dart         # Модель тарифа
│
├── test/
│   ├── services/
│   │   ├── subscription_service_test.dart # Тесты покупки
│   │   └── storage_service_test.dart      # Тесты хранилища
│   │
│   └── providers/
│       └── subscription_provider_test.dart # Тесты состояния
│
└── pubspec.yaml
```

## Запуск приложения

1. Установите зависимости:
```bash
flutter pub get
```

2. Запустите на Android-устройстве или эмуляторе:
```bash
flutter run
```

## Запуск тестов

Выполните команду для запуска всех юнит-тестов:
```bash
flutter test
```

Для запуска с покрытием кода:
```bash
flutter test --coverage
```

## Функционал

- **Онбординг** — два экрана приветствия при первом запуске
- **Paywall** — экран с двумя тарифами подписки и эмуляцией покупки
- **Главный экран** — доступен после оформления подписки
- **Персистентность** — состояние подписки сохраняется локально через SharedPreferences

## Пример работы

1. При первом запуске показывается онбординг
2. После онбординга открывается экран выбора подписки
3. Выбор тарифа и нажатие "Продолжить" эмулирует покупку
4. После покупки открывается главный экран с контентом
5. При повторном запуске пользователь сразу попадает на главный экран

---

## Улучшения относительно базового тестового задания

### Базовые требования тестового задания:
1. ✅ Онбординг (1–2 экрана) с кнопкой "Продолжить"
2. ✅ Экран Paywall с двумя подписками (месяц/год с скидкой) и кнопкой покупки
3. ✅ Главный экран с любым контентом
4. ✅ Сохранение состояния подписки в SharedPreferences/UserDefaults

### 🏗️ Архитектура (перевыполнено)

**Базовое требование:** Простое приложение с минимальной структурой

**Что реализовано:**
- **Трёхслойная Clean Architecture**: UI Layer → State Layer → Data Layer
- **Riverpod** для реактивного управления состоянием (вместо простого setState)
- **GoRouter** с автоматической redirect-логикой на основе состояния приложения
- **Изолированные сервисы** — бизнес-логика не знает о UI
- **Провайдеры** как промежуточный слой между UI и данными
- Чёткое разделение ответственности (Separation of Concerns)

### 📱 Онбординг (перевыполнено)

**Базовое требование:** 1-2 простых экрана с текстом и кнопкой

**Что реализовано:**
- **PageView с анимацией** плавного перехода между экранами
- **Индикатор прогресса** (анимированные точки) показывающий текущую страницу
- **Адаптивная кнопка** меняет текст: "Продолжить" → "Начать" на последнем экране
- **Иконки и визуальный дизайн** вместо простого текста
- **Описание ценности продукта** на каждом экране

### 💳 Paywall Screen (перевыполнено)

**Базовое требование:** Очень простой дизайн, 2 подписки, кнопка покупки

**Что реализовано:**
- **Список преимуществ подписки** (3 фичи с иконками и описаниями)
- **Visual feedback для выбранного плана:**
  - Изменение цвета фона
  - Цветная рамка
  - Чекбокс-индикатор
- **Бейдж "Выгода X%"** с автоматическим расчётом экономии
- **Расчёт цены за месяц** для годовой подписки (7990₽/год = 666₽/мес)
- **Состояние загрузки** с индикатором и визуальным feedback при покупке
- **Защита от множественных нажатий** через флаг `_isPurchasing`
- **Обработка ошибок** с SnackBar уведомлениями
- **Сохранение типа подписки** (monthly/yearly), а не просто булевого флага
- **Динамический текст кнопки** показывает цену выбранного плана

### 🧭 Навигация (перевыполнено)

**Базовое требование:** Простые переходы между экранами

**Что реализовано:**
- **GoRouter с декларативной redirect-логикой** на основе состояния:
  ```
  Нет подписки + онбординг не пройден → /onboarding
  Нет подписки + онбординг пройден → /paywall
  Есть подписка → всегда /home
  ```
- **RouterRefreshNotifier** для реактивного обновления маршрутов при изменении состояния
- **Защита от прямых переходов** на недоступные экраны
- **Автоматическая синхронизация** UI с состоянием приложения

### 💾 Хранение данных (перевыполнено)

**Базовое требование:** Сохранить факт покупки в SharedPreferences

**Что реализовано:**
- **StorageService** как изолированный слой работы с данными
- **Сохранение типа подписки** (monthly/yearly), а не только булевого флага
- **Сохранение даты покупки** для возможной валидации срока действия
- **Отдельное сохранение состояния онбординга**
- **Методы проверки валидности подписки**
- Возможность расширения для работы с реальным биллингом

### 🔄 Lifecycle Management (дополнительно)

**Базовое требование:** Не требовалось

**Что реализовано:**
- **WidgetsBindingObserver** в `main.dart`
- **Автоматическая проверка подписки** при возврате в приложение
- **Обработка AppLifecycleState.resumed** для обновления состояния
- Корректная очистка ресурсов (dispose)

### 🧪 Тестирование (дополнительно)

**Базовое требование:** Не требовалось

**Что реализовано:**
- **Юнит-тесты** для всех сервисов (StorageService, SubscriptionService)
- **Тесты провайдеров** (SubscriptionProvider)
- **Настроено покрытие кода** (`flutter test --coverage`)
- **Mock-объекты** для изоляции тестов
- Структурированная организация тестов по слоям

### 🎨 UI/UX улучшения (дополнительно)

**Базовое требование:** "Очень простой дизайн"

**Что реализовано:**
- **Material Design 3** с современными компонентами
- **Единая цветовая схема** (deepPurple) во всём приложении
- **Визуальная иерархия:**
  - Скруглённые углы (BorderRadius)
  - Тени (elevation)
  - Правильные отступы и spacing
- **Адаптивные состояния кнопок** (disabled при загрузке)
- **Плавные анимации** переходов между экранами
- **Правильная типографика** с иерархией размеров шрифтов
- **SafeArea** для корректного отображения на разных устройствах

### 🛡️ Качество кода (дополнительно)

**Что реализовано:**
- **Правильная работа с async/await** и Future
- **Проверка mounted** перед навигацией в асинхронных методах (защита от memory leaks)
- **Обработка ошибок** с try-catch-finally блоками
- **Dispose для контроллеров** (PageController) во избежание утечек памяти
- **Const конструкторы** где возможно для оптимизации производительности
- **Линтинг** с `analysis_options.yaml`
- **Понятные имена переменных** и методов
- **Комментарии** для сложной логики

### 📊 Дополнительные фичи

**Что добавлено сверх требований:**
- Эмуляция задержки покупки (2 секунды) для имитации реального API
- Визуальный индикатор процесса покупки
- Сохранение не только подписки, но и прохождения онбординга
- Возможность расширения для работы с реальными платёжными системами
- Документация в `CLAUDE.md` для разработчиков

---
